# Smart-Scheduler
The proposed system uses cloud computing to store task details and automatically trigger push notifications. A cloud-hosted backend continuously monitors scheduled times and sends notifications without manual intervention.
